<div class="steps">
  <slot />
</div>

<style is:global>
  .steps {
    --at-apply: ml-4 mb-12 pl-8 border-l;
    counter-reset: step;
    border-left-style: solid;
  }

  .steps > h3 {
    --at-apply: text-base leading-6 font-semibold -tracking-wide mt-8 mb-4 scroll-m-20;
    counter-increment: step;
  }

  .steps > h3::before {
    --at-apply: border-background bg-muted absolute inline-flex h-9 w-9
    items-center justify-center text-center -indent-px text-base leading-6
     font-medium ml-[-50px] content-[counter(step)] -mt-1 rounded-full border-4;
  }
</style>
